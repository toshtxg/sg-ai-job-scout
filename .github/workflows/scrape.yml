name: Scrape & Classify
on:
  schedule:
    - cron: '0 2 * * *'   # 2 AM UTC every day
  workflow_dispatch:          # Manual trigger

permissions:
  contents: read

concurrency:
  group: scrape-pipeline
  cancel-in-progress: false   # never overlap or kill an in-flight run

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - run: pip install -e .
      - run: python -m pipeline.run_pipeline
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_CLASSIFIER_MODEL: ${{ secrets.OPENAI_CLASSIFIER_MODEL || 'gpt-5-nano' }}
          OPENAI_SUMMARY_MODEL: ${{ secrets.OPENAI_SUMMARY_MODEL || 'gpt-5-nano' }}
